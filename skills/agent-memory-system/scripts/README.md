# Scripts

Use scripts for deterministic, repeatable steps to reduce prompt overhead.

Suggested scripts:
- `init_project.sh` — scaffold project artifact files
- `phase_status.py` — verify artifact presence and phase completion
- `release_check.sh` — run lint/typecheck/build and output validate report
